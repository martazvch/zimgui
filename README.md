# ImGUI SDL3 package for Zig

This repo packages ImGUI for SDL3 to be used in any Zig project.
It contains only the files needed for SDL3 as it fits my needs. Might add other backends in the future if needed and also ImGUI extensions.
